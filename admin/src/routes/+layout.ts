// SPA pura: tudo acontece no navegador, com o fallback index.html do adapter.
// Sem SSR porque quem autoriza e a API (AdminUser) - a tela so desenha.
export const ssr = false;
export const prerender = false;
